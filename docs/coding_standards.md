# 代码与提交规范（Unity / C#）

- C# 风格：使用 PascalCase 命名类，camelCase 命名方法与字段（私有字段以 `_` 前缀可选）。
- Unity 项目结构：`Assets/Scripts/`、`Assets/Scenes/`、`Assets/Art/`、`Assets/Audio/`。
- 提交信息：`feat(scope): 简短描述`、`fix(scope): 描述`，每个 PR 必须关联 `dev_logs` 的日期条目。
- PR 审查：每次合并主分支需有人复审并运行构建验证。
